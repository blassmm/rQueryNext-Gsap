1. npm run startdb //Activamos la db simulada con JSON server
2. npm run dev
----------------------------------------------------------
TESTING de:
* React Query en Next para peticiones (Usamos db simulada en json server)
* GSAP scroll animation